# C4. Уровень 3 — Компоненты

Детализированы два узла, определяющих успех трансформации: **портал самообслуживания**
(цель № 1 бизнеса) и **доменный дата-продукт вместе с мостом к легаси** (цель № 2).

---

## 3.1. Портал самообслуживания («витрина данных»)

```mermaid
flowchart TB
    subgraph Portal["Контейнер: Портал самообслуживания"]
        direction TB

        subgraph Front["Слой представления"]
            SEARCH["<b>Discovery UI</b><br/>поиск дата-продукта:<br/>домен, метрика, владелец"]
            BUILDER["<b>Report Builder</b><br/>конструктор: измерения,<br/>метрики, фильтры, drill-down"]
            DASH["<b>Dashboards</b><br/>готовые отчёты домена,<br/>подписки и рассылки"]
            REQ["<b>Access Request</b><br/>заявка на доступ<br/>к продукту, workflow согласования"]
        end

        subgraph Core["Ядро"]
            SEMAPI["<b>Semantic API</b><br/>метрики и измерения как код<br/>(YAML в репозитории домена)"]
            QP["<b>Query Planner</b><br/>сборка SQL по семантической модели,<br/>выбор витрины (batch / streaming)"]
            CACHE["<b>Result Cache</b><br/>кэш агрегатов,<br/>материализация частых срезов"]
            EXP["<b>Export Service</b><br/>выгрузка CSV/XLSX<br/>с водяным знаком и журналом"]
        end

        subgraph Guard["Контроль доступа"]
            AUTHZ["<b>Policy Decision Point</b><br/>OPA: решение по атрибутам<br/>(домен, роль, регион, класс данных)"]
            RLS["<b>Row/Column Filter</b><br/>подстановка предикатов<br/>в запрос до исполнения"]
            AUDIT["<b>Audit Log</b><br/>кто, какой продукт, какой срез,<br/>когда — неизменяемый журнал"]
        end
    end

    IDP["Корпоративный IdP<br/><i>Keycloak / SSO</i>"]
    TRINO["Query Engine (Trino)"]
    CATALOG["Каталог дата-продуктов"]
    LAKE["Lakehouse"]
    STREAM["Streaming-витрины (Flink)"]

    IDP -- "OIDC: атрибуты сотрудника" --> AUTHZ
    SEARCH --> CATALOG
    REQ --> CATALOG
    BUILDER --> SEMAPI --> QP
    DASH --> QP
    QP --> AUTHZ
    AUTHZ --> RLS
    RLS --> TRINO
    TRINO --> LAKE
    TRINO --> STREAM
    QP <--> CACHE
    BUILDER --> EXP --> AUDIT
    TRINO --> AUDIT

    classDef guard fill:#fdecea,stroke:#c0392b,color:#000
    classDef core fill:#eaf3fb,stroke:#2c5aa0,color:#000
    class AUTHZ,RLS,AUDIT guard
    class SEMAPI,QP,CACHE,EXP core
```

### Как работает запрос аналитика

1. Сотрудник ищет дата-продукт в **Discovery UI**; каталог возвращает только те продукты,
   которые ему разрешено видеть (сам факт существования продуктов чужого домена может
   быть скрыт).
2. В **Report Builder** он выбирает метрики и измерения из семантической модели, а не
   таблицы. «Средний чек приёма» — это одна метрика с одним определением, а не пять
   разных SQL-выражений у пяти аналитиков.
3. **Query Planner** собирает запрос и выбирает источник: batch-витрина для исторических
   срезов, streaming-витрина — для оперативных.
4. **Policy Decision Point** принимает решение по атрибутам сотрудника. **Row/Column
   Filter** дописывает предикаты в запрос *до* исполнения — пользователь физически не
   может выбрать данные вне своего доступа, даже если подставит параметры вручную.
5. Результат кэшируется; выгрузки журналируются в **Audit Log**.

### Почему портал масштабируется без переделки

| Событие | Что делает домен | Что меняется в портале |
|---|---|---|
| Появилось новое бизнес-направление (аптеки) | публикует дата-продукты и семантическую модель | ничего: продукты появляются в каталоге |
| Домену нужна новая метрика | добавляет определение в свой YAML, проходит ревью | ничего |
| Выход в новый регион | добавляет атрибут `region` в политики и данные | ничего: RLS работает по атрибутам |
| Нужен near-real-time срез | публикует streaming-версию продукта | ничего: Query Planner выбирает источник по SLA продукта |

---

## 3.2. Доменный дата-продукт и мост к легаси

Схема ниже разбирает домен «Финтех» как пример; остальные домены устроены так же.

```mermaid
flowchart LR
    subgraph Domain["Домен «Финтех» (owner: Data Product Owner домена)"]
        direction TB

        subgraph Op["Операционная часть"]
            APP["Core Banking / Кредитный конвейер"]
            OUTBOX["<b>Transactional Outbox</b><br/>событие пишется в одной транзакции<br/>с изменением состояния"]
            PUB["<b>Event Publisher</b><br/>публикация в топик домена,<br/>идемпотентность по event_id"]
        end

        subgraph Prod["Аналитическая часть — дата-продукт"]
            ING["<b>Ingestion</b><br/>подписка на собственные события,<br/>запись в bronze"]
            TRANS["<b>dbt-модели домена</b><br/>bronze → silver → gold"]
            QC["<b>Quality Gate</b><br/>тесты полноты, свежести,<br/>уникальности ключей"]
            PUBP["<b>Product Publisher</b><br/>регистрация версии продукта<br/>в каталоге, публикация SLA"]
        end
    end

    subgraph Bridge["Мост совместимости (временный)"]
        direction TB
        CDC["<b>Debezium CDC</b><br/>чтение журнала транзакций SQL Server"]
        ACL["<b>Anti-Corruption Layer</b><br/>перевод легаси-таблиц<br/>в доменные события,<br/>единицы измерения, коды справочников"]
        DLQ["<b>DLQ + карантин</b><br/>несоответствие схеме → карантин,<br/>алерт владельцу"]
    end

    KAFKA["Event Broker"]
    DWH["DWH SQL Server 2008"]
    CATALOG["Каталог дата-продуктов"]
    LAKE["Lakehouse"]
    CONSUMERS["Подписчики:<br/>другие домены, портал"]

    APP --> OUTBOX --> PUB --> KAFKA
    KAFKA --> ING --> TRANS --> QC --> LAKE
    QC --> PUBP --> CATALOG
    LAKE --> CONSUMERS
    KAFKA --> CONSUMERS

    DWH --> CDC --> ACL --> KAFKA
    ACL --> DLQ

    classDef legacy fill:#f5f0e8,stroke:#b08d57,stroke-dasharray: 5 3,color:#000
    class CDC,ACL,DLQ,DWH legacy
```

### Компоненты и их роль

| Компонент | Зачем нужен |
|---|---|
| **Transactional Outbox** | гарантирует, что событие не потеряется и не будет опубликовано без фиксации изменения — без распределённых транзакций |
| **Event Publisher** | идемпотентная публикация; повтор доставки не создаёт дубль эффекта у подписчика |
| **Ingestion → dbt → Quality Gate** | дата-продукт собирает и проверяет **сам домен**: качество — ответственность владельца, а не центральной команды DWH |
| **Product Publisher** | публикует версию продукта и SLA в каталог: потребитель видит, насколько свежи данные и кому писать при проблеме |
| **Anti-Corruption Layer** | не даёт легаси-модели SQL Server протечь в доменные события; всё уродство трансляции локализовано в одном месте и умирает вместе с DWH |
| **DLQ + карантин** | сообщение, не прошедшее валидацию схемы, не теряется и не ломает поток — оно уходит в карантин с алертом владельцу |

### Порядок вывода легаси

```mermaid
flowchart LR
    S1["<b>Этап 1</b> (0–6 мес)<br/>DWH — источник истины.<br/>CDC зеркалит изменения<br/>в события. Пилот в 1–2 доменах"]
    S2["<b>Этап 2</b> (6–18 мес)<br/>Домены — источник истины<br/>по своим данным.<br/>DWH получает обратный поток<br/>для нераспиленных отчётов"]
    S3["<b>Этап 3</b> (18–36 мес)<br/>Обратный поток отключён.<br/>DWH и Camel выведены.<br/>Аналитика — на потоках"]
    S1 --> S2 --> S3
```

Ключевая проверка перехода между этапами — **тест на паритет**: отчёт, построенный
в новой витрине, сходится с отчётом из DWH на согласованном горизонте. Пока паритет
не подтверждён, старый контур не отключается.
